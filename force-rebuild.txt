# This file exists solely to force a Docker image rebuild
# The previous builds may have used cached layers that didn't include postgresql-client
# This commit will trigger a new release and fresh Docker build
